import { describe, it, expect } from "vitest";

import { removeStorage } from "../";

describe("removeStorage", () => {
  it("should be defined", () => {
    expect(removeStorage).toBeDefined();
  });
});
